# 姓名：____________　学号：____________　日期：__________
# 下课前 5 分钟：把 C:\R课 整个文件夹拖回自己的 U 盘！
# 先填 ____ 再运行；独立书写题在空白处写代码，提交前检查漏填。

# ==============================================================================
# 第 3 讲　数据框的增删改查与排序
# 教材：Ch1.2.4　｜　数据：students.xlsx、cb_packages.xlsx、mtcars（R 内置）
# ------------------------------------------------------------------------------
# 90 分钟节奏建议
#   回合 1  增行增列：rbind() / cbind() / df$新列 <-                      12 分钟
#   回合 2  修改值与「类型污染」陷阱（本讲第一个高潮）                    12 分钟
#   回合 3  删除行列：负号索引 vs df[列] <- NULL                          10 分钟
#   回合 4  条件筛选：df[条件, ] 与 NA 陷阱（本讲第二个高潮）             14 分钟
#   回合 5  排序：order() 单条件、多条件、降序                            12 分钟
#   回合 6  综合案例：跨境电商包裹三步走 ＋ mtcars 冠亚季军               18 分钟
#   回合 7  结尾钩子：同一件事，用 dplyr 只要 4 行（只演示，不讲解）       6 分钟
#                                                              合计约 84 分钟
# ------------------------------------------------------------------------------
# 上一讲学会了「把数据读进来」，这一讲学会「把数据改成想要的样子」。
# 一句话贯穿全场：数据框的一切编辑 = 先定位（引用），再赋值。
# 下课前 5 分钟固定动作：把 C:\R课 整个文件夹拖回自己的 U 盘！
# ==============================================================================

#教师Vibe Coding示例：儿童背单词软件开发
# https://nicolas2148.github.io/English_vocabulary/





setwd("C:/R课")
library(openxlsx)

df <- read.xlsx("students.xlsx")
head(df); dim(df)

# ══════════════════════════════════════════════════════════════════════════════
# 回合 1　增行增列　⏱ 12 分钟
# ══════════════════════════════════════════════════════════════════════════════

# ── 1.1　增加一行：rbind()（row bind，按行拼接） ───────────────────────────────────────────
# ★ 正确写法：新来的那一行，本身也做成一个数据框，列名对齐
new_comer <- data.frame(学号 = "2024030141", 姓名 = "赵一鸣", 性别 = "男",
                     语文 = 88, 数学 = 91, 英语 = 79)


# ┌────────────────────────────────────────────────────────────────────────────┐
# │ ★【练习 1-1】
# 本知识点例题：参数填空；按紧邻的讲解与行尾说明完成。
# └────────────────────────────────────────────────────────────────────────────┘
?rbind

df <- rbind(df,new_comer)



# 本题作答与笔记区结束。
tail(df, 3); nrow(df)



# ── 1.2　增加一列：两种写法 ─────────────────────────────────────────────────────────────

# 写法一：cbind()（column bind，按列拼接）。注意长度必须和行数一致
weight <- round(runif(nrow(df), 45, 80), 1)     # 随机造 41 个体重


# ┌────────────────────────────────────────────────────────────────────────────┐
# │ ★【练习 1-2】
# 本知识点例题：参数填空；按紧邻的讲解与行尾说明完成。
# └────────────────────────────────────────────────────────────────────────────┘


df <- cbind(df, weight)
head(df, 3)

# 写法二：df$新列名 <- ……（★ 更常用，可读性更好，推荐）
# ____（按本知识点的讲解独立写出代码；完成后保留对象 df$总分）
head(df[, c("姓名","性别", "数学", "英语", "总分")])

# ⚠ 注意看：有两位同学的总分是 NA
df[is.na(____$____), ]
# 原因就是上一讲体检出来的那 2 个缺考。NA 会传染，第 1 讲讲过。

# 想让缺考的科目按 0 分算进去，用 rowSums() 加 na.rm
df$总分 <- rowSums(____[, c(____, ____, "英语")], na.rm = TRUE)
df[c(____, ____), c(____, "语文", "数学", "英语", "总分")]



# 本题作答与笔记区结束。
# 但要想清楚：缺考按 0 分算，公平吗？——这是数据分析里的价值判断，不是技术问题。
#留一个小问题，缺失值用平均分代替如何处理？



# ── 1.3　也可以直接用赋值造出一整列常数（循环补齐） ─────────────────────────────────────────────────


# ┌────────────────────────────────────────────────────────────────────────────┐
# │ ★【练习 1-3】
# 本知识点例题：参数填空；按紧邻的讲解与行尾说明完成。
# └────────────────────────────────────────────────────────────────────────────┘


df$班级 <- ____
head(df, 3)





# 本题作答与笔记区结束。




# ┌────────────────────────────────────────────────────────────────────────────┐
# │ ★【练习 1-4】


# ★ 动手（4 分钟）：给成绩表再加一名同学，再加一列「平均分」
# └────────────────────────────────────────────────────────────────────────────┘

新同学 <- data.frame(学号 = ____, 姓名 = ____, 性别 = ____, 语文 = ____, 数学 = ____, 英语 = ____, 体重 = ____, 总分 = ____, 班级 = ____)
df <- rbind(____, ____)
df$平均分 <- round(____ / ____, 1)






# 本题作答与笔记区结束。
#
#
#





# ══════════════════════════════════════════════════════════════════════════════
# 回合 2　修改值与「类型污染」陷阱　⏱ 12 分钟
# ══════════════════════════════════════════════════════════════════════════════
# 这个 bug 每一届都有人踩，而且踩了往往不自知。

df <- read.xlsx("students.xlsx")     # 先恢复一份干净的数据



# ── 2.1　正常的修改：定位 + 赋值 ─────────────────────────────────────────────────────────


# ┌────────────────────────────────────────────────────────────────────────────┐
# │ ★【练习 2-1】
# 本知识点例题：参数填空；按紧邻的讲解与行尾说明完成。
# └────────────────────────────────────────────────────────────────────────────┘


df$姓名[____] <- ____; head(____, 2)
df$语文[____] <- ____;                     head(____, 2)
df[____, ____] <- ____;                head(df, 2)      # 用 [行, 列] 定位也可以
df$性别 <- ____;                   head(____, ____)      # 整列一起改（循环补齐）






# 本题作答与笔记区结束。



# ── 2.2　★ 事故现场：只是多打了一对引号 ──────────────────────────────────────────────────────
df <- read.xlsx("students.xlsx")     # 再恢复一次

class(df$语文)          # 现在是 numeric，正常
# 我猜：一个带引号的数会影响整列的类型吗？ ____
df$语文[1] <- "99"
head(df, 3)             # 表面上完全看不出区别！

class(df$语文)          # ★ 整整一列，全被拉成了 character
str(df)                 # 用 str() 看得更清楚

# 后果有多严重？
mean(df$语文, na.rm = TRUE)     # 返回 NA，并给一个警告：参数不是数值型
# df$语文 * 2                    # 这一行会直接报错，想看的话手动去掉 # 单独运行
# sort(df$语文)[1:5]             # 排序也全乱了：字符是按字典序排的，"100" 会排在 "43" 前面



# ── 2.3　病根：向量的元素类型必须统一（第 1 讲 c(1, "a", TRUE) 那个坑） ─────────────────────────────
# 数据框的一列就是一个向量。往里塞一个字符，整列就得迁就它，一起变成字符。



# ── 2.4　怎么修 ───────────────────────────────────────────────────────────────────


# ┌────────────────────────────────────────────────────────────────────────────┐
# │ ★【练习 2-2】
# 本知识点例题：参数填空；按紧邻的讲解与行尾说明完成。
# └────────────────────────────────────────────────────────────────────────────┘


df$语文 <- as.numeric(____$____)     # 强制转回数值型






# 本题作答与笔记区结束。
class(df$语文); mean(df$语文, na.rm = TRUE)



# ── 2.5　同一个病的另一种发作方式：用 c() 拼一行往里塞 ─────────────────────────────────────────────
df <- read.xlsx("students.xlsx")
坏行 <- c("2024030141", "赵一鸣", "男", 88, 91, 79)   # c() 里混了字符和数字
class(坏行)                                            # 整个向量已经变成 character 了


# ┌────────────────────────────────────────────────────────────────────────────┐
# │ ★【练习 2-3】
# 本知识点例题：参数填空；按紧邻的讲解与行尾说明完成。
# └────────────────────────────────────────────────────────────────────────────┘


df2 <- rbind(____, ____)
df2





# 本题作答与笔记区结束。
str(df2)       # 三科成绩全变成了字符型
# ——所以回合 1 才强调：新增行要做成 data.frame，不要用 c()。

# ★ 养成习惯：每次改完数据框，顺手跑一次 str(df)，30 秒排除一小时的 debug。



# ┌────────────────────────────────────────────────────────────────────────────┐
# │ ★【练习 2-4】


# ★ 动手（4 分钟）：亲手复现这个 bug —— 把某位同学的数学成绩改成 "88"（带引号），
#   用 class() 看清发生了什么，再把它修回来
# └────────────────────────────────────────────────────────────────────────────┘

d <- read.xlsx("students.xlsx")
d$数学[____] <- ____
class(d$数学); str(d)
d$数学 <- ____(d$数学)
class(d$数学)






# 本题作答与笔记区结束。
#
#
#





# ══════════════════════════════════════════════════════════════════════════════
# 回合 3　删除行列　⏱ 10 分钟
# ══════════════════════════════════════════════════════════════════════════════

df <- data.frame(姓名 = c("张三", "李四", "王五", "赵六"),
                 成绩 = c(66, 77, 88, 99),
                 籍贯 = c("北京", "上海", "广州", "深圳"))
df



# ── 3.1　删行：只有一种办法 —— 负号索引 ─────────────────────────────────────────────────────


# ┌────────────────────────────────────────────────────────────────────────────┐
# │ ★【练习 3-1】
# 本知识点例题：参数填空；按紧邻的讲解与行尾说明完成。
# └────────────────────────────────────────────────────────────────────────────┘


df2 <- df[-____, ];    df2       # 删掉第 3 行（王五）
df2 <- df[-(____:____), ]; df2      # 删掉前两行






# 本题作答与笔记区结束。
# ⚠ 删完之后行号是断的（4 变不成 3），这是 R 的行名机制，不影响使用


# ── 3.2　删列：两种办法都行 ─────────────────────────────────────────────────────────────


# ┌────────────────────────────────────────────────────────────────────────────┐
# │ ★【练习 3-2】
# 本知识点例题：参数填空；按紧邻的讲解与行尾说明完成。
# └────────────────────────────────────────────────────────────────────────────┘


df3 <- df[-____];      df3       # 负号索引，没有逗号 = 按列操作
df4 <- df; df4[____] <- NULL; df4          # 赋 NULL，直接把这一列抹掉
# ____（按本知识点的讲解独立写出代码；完成后保留对象 df5）  # 用列名，最好读






# 本题作答与笔记区结束。



# ── 3.3　★ 为什么删列比删行灵活？ ─────────────────────────────────────────────────────────
# 因为数据框本质上是「等长向量组成的列表」。

# 列 = 列表的一个分量，抽掉一个分量，列表还是列表，天经地义。

# 行 = 横跨所有分量的一个切片，本来就不是一个独立对象，自然没法直接抹掉。

# 小结：数据框是「一列一列粘起来的」，不是「一行一行摞起来的」。

length(df)      # 数据框的「长度」是列数
ncol(df)        # 同一个数



# ┌────────────────────────────────────────────────────────────────────────────┐
# │ ★【练习 3-3】


# ★ 动手（3 分钟）：把上面这个 df 删成只剩姓名和成绩两列、只剩前两行
# └────────────────────────────────────────────────────────────────────────────┘

df[____, c(____, ____)]






# 本题作答与笔记区结束。





# ══════════════════════════════════════════════════════════════════════════════
# 回合 4　条件筛选　⏱ 14 分钟
# ══════════════════════════════════════════════════════════════════════════════
# 「查」是数据分析中出现频率最高的动作。核心还是那句话：定位 + 引用。

df <- read.xlsx("students.xlsx")



# ── 4.1　单条件 ───────────────────────────────────────────────────────────────────


# ┌────────────────────────────────────────────────────────────────────────────┐
# │ ★【练习 4-1】
# 本知识点例题：参数填空；按紧邻的讲解与行尾说明完成。
# └────────────────────────────────────────────────────────────────────────────┘


df[____$____ > ____, ]                    # 数学 90 分以上的同学
df[____$____ == ____, ]                 # 全体女生
nrow(____[____$____ == "女", ])           # 有多少人

# 逗号后面还可以指定要哪几列
df[____$____ > ____, c("姓名", "数学")]






# 本题作答与笔记区结束。



# ── 4.2　多条件：& 是「且」，| 是「或」 ─────────────────────────────────────────────────────


# ┌────────────────────────────────────────────────────────────────────────────┐
# │ ★【练习 4-2】
# 本知识点例题：参数填空；按紧邻的讲解与行尾说明完成。
# └────────────────────────────────────────────────────────────────────────────┘


df[____$____ == ____ & df$数学 > 85, c("姓名", "性别", "数学")]
df[____$____ > ____ | df$数学 < 50, c("姓名", "数学")]      # 两头的极端情况
df[____$____ >= ____ & df$数学 < 90, c("姓名", "数学")]

# %in% ：属于其中之一，比写一长串 | 干净得多
df[____$____ %in% c(____, "赵敏", "蒋雯"), ]






# 本题作答与笔记区结束。



# ── 4.3　★ 本讲第二个要点：NA 会在筛选里搞出「幽灵行」 ─────────────────────────────────────────────
# 我猜：筛选条件含 NA 时，会留下什么样的行？ ____
df[df$语文 > 90, ]
# 仔细看输出 —— 中间夹着一行全是 NA 的记录！
# 原因：吴倩的语文是 NA，NA > 90 的结果既不是 TRUE 也不是 FALSE，而是 NA。
# 方括号遇到 NA 索引，就还你一行 NA。

df$语文 > 90                # 亲眼看一下这个逻辑向量里的 NA

# 两种解法：


# ┌────────────────────────────────────────────────────────────────────────────┐
# │ ★【练习 4-3】
# 本知识点例题：参数填空；按紧邻的讲解与行尾说明完成。
# └────────────────────────────────────────────────────────────────────────────┘


df[which(____$____ > ____), ]        # 解法一：which() 只返回 TRUE 的位置，NA 自动被丢掉
subset(____, ____ > ____)            # 解法二：subset() 内部替你处理了 NA，而且列名不用写 df$






# 本题作答与笔记区结束。

# ⚠ 这个坑在真实数据里极其常见：筛完之后行数对不上、汇总结果莫名其妙。
#   养成习惯：筛选之前先看一眼缺失值情况。
colSums(is.na(df))    #very very important!!!





# ┌────────────────────────────────────────────────────────────────────────────┐
# │ ★【练习 4-4】


# ★ 动手（5 分钟）：用 R 内置的 mtcars 数据集，筛选出「4 缸且马力大于 90」的汽车
#   （mtcars：32 款汽车的 11 项指标，cyl 是气缸数，hp 是马力，mpg 是燃油经济性）
# └────────────────────────────────────────────────────────────────────────────┘

mtcars[____ & ____, ]






# 本题作答与笔记区结束。
head(mtcars); ?mtcars
#
#
#





# ══════════════════════════════════════════════════════════════════════════════
# 回合 5　排序　⏱ 12 分钟
# ══════════════════════════════════════════════════════════════════════════════
# 第 1 讲讲过 sort() 和 order() 的区别：sort() 给「值」，order() 给「位置」。
# 给整张表排序，只能用 order() —— 因为要让姓名、学号跟着成绩一起动。

df <- read.xlsx("students.xlsx")



# ── 5.1　单条件排序 ─────────────────────────────────────────────────────────────────


# ┌────────────────────────────────────────────────────────────────────────────┐
# │ ★【练习 5-1】
# 本知识点例题：参数填空；按紧邻的讲解与行尾说明完成。
# └────────────────────────────────────────────────────────────────────────────┘


order(____$____)                           # 返回的是行号
head(____[order(____$____), ])               # 把行号塞进方括号 → 按数学升序排列整张表
head(____[order(____$____, decreasing = TRUE), ])    # 降序






# 本题作答与笔记区结束。



# ── 5.2　负号降序：更短的写法（★ 只对数值列有效，字符列会报错） ──────────────────────────────────────────


# ┌────────────────────────────────────────────────────────────────────────────┐
# │ ★【练习 5-2】
# 本知识点例题：参数填空；按紧邻的讲解与行尾说明完成。
# └────────────────────────────────────────────────────────────────────────────┘


head(____[order(-____$____), ])






# 本题作答与笔记区结束。



# ── 5.3　多条件排序：前面的是主关键字，前面相同了再看后面 ──────────────────────────────────────────────


# ┌────────────────────────────────────────────────────────────────────────────┐
# │ ★【练习 5-3】
# 本知识点例题：参数填空；按紧邻的讲解与行尾说明完成。
# └────────────────────────────────────────────────────────────────────────────┘


head(____[order(____$____, df$数学), ], 10)
# 先按性别排（女在前，因为按拼音字典序），性别相同再按数学升序

# ★ 常见需求：一个升序、一个降序 —— 给要降序的那个加负号
head(____[order(____$____, -df$数学), ], 10)






# 本题作答与笔记区结束。



# ── 5.4　排序结果记得存下来，否则白排 ────────────────────────────────────────────────────────


# ┌────────────────────────────────────────────────────────────────────────────┐
# │ ★【练习 5-4】
# 本知识点例题：参数填空；按紧邻的讲解与行尾说明完成。
# └────────────────────────────────────────────────────────────────────────────┘


new_df <- df[order(-____$____), ]
head(new_df, 5)

# ⚠ NA 在排序里默认排到最后，不像筛选那样制造幽灵行，这一点是友好的
tail(____[order(____$____), ], 3)






# 本题作答与笔记区结束。



# ┌────────────────────────────────────────────────────────────────────────────┐
# │ ★【练习 5-5】


# ★ 动手（5 分钟）：对 mtcars 按 cyl 升序排列，cyl 相同时按 mpg 降序排列
# └────────────────────────────────────────────────────────────────────────────┘

mtcars[order(____, ____), ]






# 本题作答与笔记区结束。
#
#
#





# ══════════════════════════════════════════════════════════════════════════════
# 回合 6　综合案例　⏱ 18 分钟
# ══════════════════════════════════════════════════════════════════════════════



# ── 6.1　案例：跨境电商包裹的重点查验名单（教师演示） ────────────────────────────────────────────────
# 背景：某跨境电商监管中心一天进来 3000 个包裹，需要挑出高价值包裹重点盯防。
# 3000 行已经不适合逐行查看或手工处理；下面用向量化筛选、排序和整列赋值一次完成。
cb <- read.xlsx("cb_packages.xlsx")
head(cb);tail(cb, 3); # 只看头尾，不直接打印整张大表
dim(cb); str(cb);colSums(is.na(cb)) #检查缺失值

                        
table(cb$商品类别); table(cb$入境口岸) # 快速了解分类分布

summary(cb[, c("申报价值", "通关时间")])

format(object.size(cb), units = "auto")  #查看数据体量

# 先做最基本的数据体检：行数、编号唯一性、关键字段类型
stopifnot(nrow(cb) == 3000,           #以下必须全部成立，否则报错
          anyDuplicated(cb$包裹编号) == 0,
          is.numeric(cb$申报价值),
          is.numeric(cb$通关时间))

# 第 1 步　筛选出申报价值大于 4000 元的包裹
  target <- cb[which(!is.na(cb$申报价值) & cb$申报价值 > 4000), , drop = FALSE]
  nrow(target)

# 第 2 步　按通关时间从短到长排序；时间相同时按包裹编号排序，保证结果稳定
  target <- target[order(target$通关时间, target$包裹编号, na.last = TRUE), , drop = FALSE]
  head(target)

# 第 3 步　新增一列「监管状态」，统一标注为「重点查验」
  target$监管状态 <- "重点查验" ;target
  rownames(target) <- NULL  ;head(target) #删掉已无意义的行名

# 结构化结果输出

cat("重点查验包裹：", nrow(target), "个，占全部包裹的",
    sprintf("%.1f%%", 100 * nrow(target) / nrow(cb)), "\n")

# 第 4 步　把结果交出去（上一讲学的）
write.xlsx(target, "重点查验名单.xlsx", overwrite = TRUE)
file.exists("重点查验名单.xlsx")

# 回头看：筛选 → 排序 → 加列 → 导出。这就是一个完整的、能交差的数据分析任务。
# 三行核心代码仍然都是「定位 + 赋值」；数据从 100 行增至 3000 行，写法不变。



# ┌────────────────────────────────────────────────────────────────────────────┐
# │ ★【综合练习 1】
# └────────────────────────────────────────────────────────────────────────────┘



# ── 6.2　课堂练习：mtcars 冠亚季军（学生动手） ────────────────────────────────────────────────
# 第 1 步　筛选出气缸数 cyl 为 4 的汽车
# 第 2 步　按燃油经济性 mpg 从大到小排列
# 第 3 步　只保留前 3 行，且只保留 mpg 这 1 列
# 第 4 步　新增一列「奖项」，依次标注冠军、亚军、季军
#
#
#
#
#
#
#





# ══════════════════════════════════════════════════════════════════════════════
# 回合 7　结尾钩子：同一件事，用 dplyr 只要 4 行　⏱ 6 分钟
# ══════════════════════════════════════════════════════════════════════════════



# ── 7.1　结尾钩子：同一件事，用 dplyr 只要 4 行 ──────────────────────────────────────────────
# 只演示，不讲解。下一讲我们就从这里开始。

library(dplyr)

# 回合 6 的电商案例，base R 写法（显式写出数据框和列名）：
#   target <- cb[which(!is.na(cb$申报价值) & cb$申报价值 > 4000), , drop = FALSE]
#   target <- target[order(target$通关时间, target$包裹编号, na.last = TRUE), , drop = FALSE]
#   target$监管状态 <- "重点查验"

# dplyr 写法：
结果 <- cb %>%
  filter(申报价值 > 4000) %>%          # 筛
  arrange(通关时间, 包裹编号) %>%      # 排（并列时按包裹编号，和 base R 保持一致）
  mutate(监管状态 = "重点查验")        # 加列
head(结果)

# %>% 读作「然后」：拿 cb，然后筛，然后排，然后加一列。
# 从左到右读下来，就是一句人话 —— 这就是下一讲要学的东西。
#
# ⚠ 机房是 R 4.0.3，管道只能写 %>%，不能写 |>（那是 R 4.1 才有的）。
#   快捷键：Ctrl + Shift + M

# 两种写法结果完全一样，验证一下（比对包裹编号的顺序）：
identical(结果$包裹编号, target$包裹编号)



# 课后作业 ---------------------------------------------------------------------
# 用 cb_packages.xlsx 完成，全部用本讲学的 base R 写法（下一讲再用 dplyr 重做一遍）：
# 【作业 1】 这批包裹一共有几个？分几个商品类别？分别从哪几个口岸入境？
# 【作业 2】 挑出「数码电子」类、且通关时间超过 20 小时的包裹，按申报价值降序排列。
# 【作业 3】 给全表新增一列「价值等级」：申报价值 3000 元以上标「高」，其余标「普通」。
#    （提示：先 df$价值等级 <- "普通"，再用条件筛选定位后赋值为「高」）
# 【作业 4】 把第 3 题的结果存成 Excel，交上来。
# 【作业 5】 思考题：如果申报价值那一列里混进了一个 "4500"（带引号），
#    第 2 题和第 3 题会出什么问题？为什么？


# ══════════════════════════════════════════════════════════════════════════════
# 课堂小结（下课前 3 分钟填，随 .R 文件一起提交）
# ══════════════════════════════════════════════════════════════════════════════
# 今天我最不确定的一个点是：
#
#
# 我卡住过的地方（哪个练习编号）：
#
#
